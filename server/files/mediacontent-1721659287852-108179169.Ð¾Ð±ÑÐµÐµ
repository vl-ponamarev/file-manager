😀️//В JavaScript есть 8 основных ТИПОВ ДАННЫХ.
=object для более сложных структур данных.
Примитивы
=number для любых чисел: целочисленных или чисел с плавающей точкой; целочисленные значения ограничены диапазоном ±(253-1).
=bigint для целых чисел произвольной длины.
=string для строк. Строка может содержать ноль или больше символов, нет отдельного символьного типа.
=boolean для true/false.
=null для неизвестных значений – отдельный тип, имеющий одно значение null.
	null > 0; // false
	null == 0; // false
	null >= 0; // true
	
	Для null возвращается "object" – это ошибка в языке, на самом деле это не объект.
	
	Значения null и undefined равны == друг другу и не равны любому другому значению.
	undefined == null → true
	
=undefined для неприсвоенных значений – отдельный тип, имеющий одно значение undefined.
=symbol для уникальных идентификаторов.

//Правила численного преобразования:

😀️Значение		Преобразуется в…
undefined		NaN
null			0
true / false		1 / 0
string	Пробельные символы (пробелы, знаки табуляции \t, знаки новой строки \n и т. п.) по краям обрезаются. Далее, если остаётся пустая строка, то получаем 0, иначе из непустой строки «считывается» число. При ошибке результат NaN.

//Логическое – Происходит в логических операциях. Может быть вызвано с помощью Boolean(value).

Подчиняется правилам:

Значение	Становится…
0, null, undefined, NaN, ""	false
любое другое значение	true

😀️Сравнение разных типов
При сравнении значений разных типов JavaScript приводит каждое из них к числу.
5 > 4 → true
"ананас" > "яблоко" → false
"2" > "12" → true //посимвольное сравнение строк
undefined == null → true
undefined === null → false
null == "\n0\n" → false
null === +"\n0\n" → false

😀️Преобразование типов
1)"" + 1 + 0 = "10" // (1)
2)"" - 1 + 0 = -1 // (2)
3)true + false = 1
4)6 / "3" = 2
5)"2" * "3" = 6
6)4 + 5 + "px" = "9px" - конкатенация(сложение строк)
7)"$" + 4 + 5 = "$45" - конкатенация
8)"4" - 2 = 2
9)"4px" - 2 = NaN
10)"  -9  " + 5 = "  -9  5" // (3) - конкатенация
11)"  -9  " - 5 = -14 // (4)
12)null + 1 = 1 // (5)
undefined + 1 = NaN // (6)
" \t \n" - 2 = -2 // (7)
(1)Сложение со строкой "" + 1 преобразует 1 к строке: "" + 1 = "1", и в следующем случае "1" + 0 работает то же самое правило.
(2)Вычитание - (как и большинство математических операторов) работает только с числами, пустая строка "" приводится к 0.
(3)Сложение со строкой превращает число 5 в строку и добавляет к строке.
(4)Вычитание всегда преобразует к числу, значит строка " -9 " становится числом -9 (пробелы по краям обрезаются).
(5)null становится 0 после численного преобразования.
(6)undefined становится NaN после численного преобразования.
(7)Пробельные символы, такие как \t и \n, по краям строки игнорируются при преобразовании в число, так что строка " \t \n", аналогично пустой строке, становится 0 после численного преобразования.

//Список логических операторов 

😀️&& возвращает первое ложное значение. Или последнее, если ничего не найдено.
😀️Цепочка ИЛИ || возвращает первое истинное значение или последнее, если такое значение не найдено.


😀️оператор ! (логическое отрицание) используется для инвертирования значения выражения. Если выражение истинно, то ! вернет false, а если выражение ложно, то ! вернет true. 
const x = 10;
const y = !x; // y = false

😀️Оператор !! (двойное логическое отрицание) используется для приведения значения к логическому типу (true или false). Он инвертирует значение выражения дважды, что приводит к преобразованию любого значения к его логическому эквиваленту. Если значение истинно, то !! вернет true, а если значение ложно, то !! вернет false. Например:

const x = 10;
const y = !!x; // y = true

😀️//ОПЕРАТОР «логическое НЕ», или оператор логического отрицания !!
!!null            // false
!!undefined       // false
!!false           // false
!!true            // true
!!""              // false
!!"string"        // true
!!0               // false
!!1               // true
!!{}              // true
!![]              // true

😀️//ОПЕРАТОР НУЛИВОГО СЛИЯНИЯ
Результат выражения a ?? b будет следующим:
если a определено, то a,
если a не определено, то b.

let user;
alert(user ?? "Аноним"); // Аноним (user не существует)

😀️//Оператор опциональной цепочки ?.
Obj?.prop - проверяет левую часть на null/undefined и позволяет продолжить вычисление, если это не так. Позволяет безопасно получить доступ к вложенным свойствам.

😀️Оператор typeof имеет две формы: typeof x или typeof(x).

😀️Поднятие (hoisting) — это поведение, при котором функцию или переменную можно использовать до объявления.

😀️ с помощью Number.isNaN() мы проверяем, является ли переданное значение числом и равняется ли оно NaN
с помощью метода isNaN мы проверяем, что переданное значение не является числом

const numbers = [1, 2, 3, 4, NaN]
console.log(numbers.findIndex(Number.isNaN)) // 4
console.log(numbers.includes(Number.isNaN)) // true

😀️В JavaScript, NaN (Not-a-Number) имеет тип number
JavaScript использует формат чисел с плавающей запятой, определенный стандартом IEEE 754.
В этом стандарте, NaN является специальным значением, которое указывает, что результат математической операции не является числом, но все еще считается значением числового типа.


// деструктуризация вложенного объекта
😀️ const obj = {
  street: 'Vavilova',
  house: 12,
  user: 'Vasya',
  addres: { address: 'some address' }
};

const { house, addres: { address } } = obj;

console.log(house);   // 12
console.log(address); // 'some address'


😀️В JavaScript оператор void используется для явного указания того, что результат выражения или функции должен быть равен undefined. Оператор void принимает выражение в качестве аргумента и возвращает undefined.

Когда void применяется к выражению, оно вычисляется, но результат игнорируется, и вместо него возвращается undefined. Это может быть полезно в ситуациях, когда вы хотите убедиться, что результат выполнения функции не используется или не имеет побочных эффектов.

Пример использования void:


function doSomething() {
  // Выполнение каких-то действий...
  return 42;
}

var result = void doSomething();
console.log(result); // Вывод: undefined


😀️тернарный оператор
Это аналогичная запись if else. Оператор представлен знаком вопроса ?. Его
также называют «тернарный», так как этот оператор, единственный в своём роде, имеет три аргумента.


😀️
С помощью Number.isNaN() мы проверяем, является ли переданное значение числом и равняется ли оно NaN. 
 С помощью метода isNaN мы проверяем, что переданное значение не является числом. 
 
 😀️ Почему typeof null === ‘object’? Это общепризнанное поведение JS. Многие считают его багом, попытаюсь объяснить почему. 

Любая переменная хранит свое значение в виде последовательности битов. Из 32 бит, отведенных для хранения значения переменной, решили 3 выделить под хранение ее типа.

Мы понимаем, что всего возможно 2^3 = 8 вариантов типов и, по счастливой случайности, 000 выделили для типа object. Если вы когда-то встречались с понятием «нулевой указатель», то знаете, что он представляет собой переменную — последовательность нулей. Догадываетесь? Проверяя у этой переменной тип, оператор typeof в JavaScript встречает три нулика и понимает, что перед нами объект.




ECMAScript (ES) - язык программирования
ECMA-262 - спецификация языка ECMAScript
ES 2016 года, 7ое издание => ES2016 = ES7, а ES2015 = ES6
JavaScript - реализация ES
http://www.ecma-international.org/ecma-262/6.0/
https://github.com/tc39/ecma262#ecmascript
